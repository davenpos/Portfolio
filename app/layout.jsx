import "@/assets/global.css"
import Header from '@/components/Header'
import Footer from '@/components/Footer'

export const metadata = {
  title: 'Next.js',
  description: 'Generated by Next.js',
  name: "width=device-width, initial-scale=1.0"
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-green-600 min-h-[100dvh] flex flex-col drop-shadow-md">
        <Header />
        <main className="flex-1 bg-orange-200 opacity-90 rounded-[15px] p-3 w-full max-w-[1000px] mx-auto shadow-lg">{children}</main>
        <Footer />
      </body>
    </html>
  )
}
